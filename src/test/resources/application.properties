jwt.secret=dGhpc2lzYXRlc3RzZWNyZXRrZXl0aGF0aXN2ZXJ5bG9uZ2FuZHNlY3VyZQ==
jwt.expiration=3600000
jwt.refresh-expiration=86400000
jwt.refresh-cookie-secure=false
jwt.refresh-cookie-same-site=Strict
rate-limit.key-secret=dGhpc2lzYXRlc3RzZWNyZXRrZXl0aGF0aXN2ZXJ5bG9uZ2FuZHNlY3VyZQ==
spring.cache.type=simple
bucket4j.enabled=false
spring.mail.host=${MAIL_HOST:}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.frontend-url=${FRONTEND_URL:http://localhost:4200}
app.frontend-path-verify-email=${FRONTEND_PATH_VERIFY_EMAIL:/verify-email}
app.frontend-path-reset-password=${FRONTEND_PATH_RESET_PASSWORD:/reset-password}
app.mail.from=${MAIL_FROM:from}
app.email-use=${APP_EMAIL_USE:true}
########### Keycloak test defaults ###########
keycloak.realm=music-player
keycloak.issuer-uri=http://localhost:8081/realms/music-player
keycloak.internal-url=http://localhost:8081
keycloak.jwk-set-uri=http://localhost:8081/realms/music-player/protocol/openid-connect/certs
keycloak.client-id=music-player-backend
keycloak.client-secret=test-client-secret
keycloak.admin-client-id=music-player-admin
keycloak.admin-client-secret=test-admin-client-secret
keycloak.token-uri=${keycloak.internal-url}/realms/${keycloak.realm}/protocol/openid-connect/token
keycloak.logout-uri=${keycloak.internal-url}/realms/${keycloak.realm}/protocol/openid-connect/logout
keycloak.revoke-uri=${keycloak.internal-url}/realms/${keycloak.realm}/protocol/openid-connect/revoke
keycloak.admin-users-uri=${keycloak.internal-url}/admin/realms/${keycloak.realm}/users
spring.security.oauth2.resourceserver.jwt.issuer-uri=${keycloak.issuer-uri}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${keycloak.jwk-set-uri}
