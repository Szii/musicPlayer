{
  "branches": ["master"],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "publishCmd": "docker build -t ${env.DOCKER_NAME}/${env.DOCKER_REPO_BE}:${nextRelease.version} -t ${env.DOCKER_NAME}/${env.DOCKER_REPO_BE}:latest . && docker push ${env.DOCKER_NAME}/${env.DOCKER_REPO_BE}:${nextRelease.version} && docker push ${env.DOCKER_NAME}/${env.DOCKER_REPO_BE}:latest"
      }
    ],
    "@semantic-release/github"
  ]
}